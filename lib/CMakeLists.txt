add_subdirectory(solver)
# TODO: remove this when the solver is ready
# add_subdirectory(c-api)
if(CLINGCON_BUILD_PYTHON)
    # TODO: remove this when the c-api is ready
    # add_subdirectory(python-api)
endif()
